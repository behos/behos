Currently working at [DataDog](https://datadoghq.com).

Interests:
* Rust :crab:
* Distributed Systems
* Datastores

> Nothing is built on :haskell: ; all is built on :rust:, but we must build as if the :rust: were :haskell: .
